# iFare 基金會網站 — 功能與業務流程說明

> 版本：v2.1（2026-05-19 補充版）
> 建立日期：2026-04-14
> 整併日期：2026-04-28
> 負責人：昀臻
>
> **檔案來源**：本檔由 `iFare_業務邏輯說明.md` 改名而來。
> **預留合併**：未來若新增「後台功能說明」相關文件，建議併入本檔（避免業務邏輯與實際功能脫節）。

---

## 目錄

### 一、基金會與網站定位
- 1.1 長穩社福慈善基金會簡介
- 1.2 iFare 網站的目標與受眾
- 1.3 前台 vs 後台的角色區分

### 二、前台功能 — 使用者視角
- 2.1 首頁（index）
  - Hero 輪播 / 最新消息摘要 / 福利查詢入口
- 2.2 i-Fare 福利查詢（核心功能）
  - 2.2.1 搜尋流程（選條件 → 查詢 → 結果 → 詳情 → 洽辦）
  - 2.2.2 篩選條件說明（政策類別 / 戶籍地 / 受助對象 / 經濟條件 / 關鍵字）
  - 2.2.3 查詢結果的呈現邏輯
  - 2.2.4 福利詳情頁內容結構
  - 2.2.5 洽辦單位的地區分類邏輯
- 2.3 最新消息
  - 列表 / 內頁 / 發布與下架邏輯
- 2.4 福利專欄
  - 2.4.1 福利文章（依政策類別 / 關鍵字篩選）
  - 2.4.2 懶人包（圖片式內容）
  - 2.4.3 文章與福利政策的關聯
- 2.5 關於長穩
  - 基金會介紹 / 三大核心 / 團隊成員
- 2.6 公益夥伴
  - 合作單位展示邏輯

### 三、後台功能 — 管理員視角
- 3.1 登入與權限
  - 三種角色能做什麼、不能做什麼
- 3.2 內容管理 CRUD 通用流程
  - 列表頁 → 搜尋篩選 → 新增/編輯 → 詳情 → 刪除
  - 狀態欄位：發布 / 下架 / 草稿
  - 發布時間 / 下架時間的自動控制
- 3.3 各模組的業務差異
  - 3.3.1 最新消息 — 純文字+富文本
  - 3.3.2 福利文章 — 關聯政策類別+關鍵字
  - 3.3.3 懶人包 — 多圖上傳排序
  - 3.3.4 福利政策 — 最複雜（多代碼關聯+資格條件+洽辦單位）
  - 3.3.5 常見問題 — 問答配對
  - 3.3.6 洽辦單位 — 地區+多單位明細
  - 3.3.7 公益夥伴 — Logo+連結
- 3.4 代碼維護（6 種）
  - 什麼是代碼？為什麼需要維護？
  - 代碼與其他模組的關聯關係
- 3.5 帳號管理（僅管理者）
- 3.6 圖片管理
- 3.7 數據分析 Dashboard
  - 訪客統計 / 流量圖表

### 四、資料關聯地圖
- 4.1 福利政策是核心：Policy ↔ Code 表 ↔ OfficeUnit 的關係
- 4.2 文章與代碼的關聯
- 4.3 前台查詢條件如何對應後台代碼

### 五、業務規則與例外
- 5.1 發布/下架時間邏輯
- 5.2 前台顯示條件（state 欄位判斷）
- 5.3 圖片上傳限制
- 5.4 富文本內容的處理方式

---

## 2026-05-19 補充：PageManagement / PageBuilder 業務流程

本段補上「後台新增頁面」目前的實際流程，對應 PageBuilder PoC v3。這個流程目前可支援本機與 staging 驗證；正式長期架構仍建議改成 .NET API + 正式圖片儲存。

### 3.8 後台頁面管理（PageManagement）

**使用角色**

- 管理者：可新增、編輯、發布、下架、刪除動態頁面。
- 編輯者：可維護內容頁，是否可發布需依正式權限設定確認。
- 檢視者：僅可瀏覽，不應進行內容異動。

**新增頁面流程**

1. 進入後台「頁面管理」。
2. 點「快速新增頁面」或新增按鈕。
3. 選擇模板：空白、介紹頁、圖文、導流等。
4. 填寫基本資料：頁面標題、slug、SEO 描述、發布狀態。
5. 使用 PageBuilder 編輯 sections：
   - Hero / 標題區
   - 文字段落
   - 圖文並列
   - CTA / 立即行動
   - 補充資訊
6. 使用前台預覽檢查桌機 / 平板 / 手機版面。
7. 儲存。
8. 後台將內容同步到前台 Nuxt server route。
9. 前台使用 `/<slug>` 顯示已發布頁面。

**圖片上傳流程**

1. 在圖文區塊選擇「選擇本機圖片」。
2. 後台上傳到 `VITE_FRONTEND_ASSET_UPLOAD_URL`，預設為 `/api/dynamic-assets`。
3. 前台以 `/api/dynamic-assets/<filename>` 讀取圖片。
4. 儲存頁面後，前台重整即可看到圖片。

**不能使用的圖片來源**

- `C:\Users\...\image.png`
- `file://...`
- `IPv4://...` / `IPv6://...`
- 僅存在於個人電腦但沒有上傳到網站的檔案

以上來源在正式網站瀏覽器中無法讀取，應改用圖片上傳功能。

### 3.9 前台動態頁顯示規則

前台 `/<slug>` 會依下列條件顯示 PageBuilder 頁面：

- slug 存在於 `dynamic-pages.json`。
- 狀態為 `published`。
- 若有發布 / 下架時間，當下時間需落在可顯示區間。
- 對應 sections 可被 `DynamicPageRenderer` 正常渲染。

若任一條件不符合，前台應回 404 或顯示對應 fallback。

### 5.5 PageBuilder 例外處理

| 情境 | 系統行為 | 處理方式 |
|------|----------|----------|
| 前台 dev server 未啟動 | 後台仍可儲存 localStorage，但同步失敗 | 啟動前台後重新儲存或發布一次 |
| 圖片上傳失敗 | 顯示錯誤，不應讓儲存按鈕一直 loading | 檢查 `VITE_FRONTEND_ASSET_UPLOAD_URL` 與前台 server |
| 前台破圖 | 顯示 fallback 文案 | 確認圖片 URL 是否為 `/api/dynamic-assets/...` |
| slug 重複或撞靜態路由 | 目前仍需人工避免 | 後續應補即時 slug 衝突檢查 |
| 遠端部署後內容消失 | 多半是 runtime 資料被覆蓋 | 保留 / 備份 `server/data/` |

### 5.6 部署前驗收流程

- 後台新增一頁並儲存。
- 圖文區塊上傳一張圖片。
- 點「前往前台預覽」確認 `/<slug>` 可顯示。
- 切換草稿後確認前台不顯示。
- 用桌機、平板、手機 viewport 檢查 Hero、圖文、CTA、Footer 不重疊。
- 確認 `server/data/dynamic-pages.json` 與 `server/data/dynamic-assets/` 已納入部署保留策略。

---

## 變更紀錄

| 版本 | 日期 | 變更內容 |
|------|------|----------|
| v2.0 | 2026-04-28 | 整併業務邏輯說明為功能與業務流程文件 |
| v2.1 | 2026-05-19 | 補充 PageManagement / PageBuilder 新增頁面、圖片上傳、前台顯示與部署前驗收流程 |
