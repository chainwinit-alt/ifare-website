# iFare 開發交接紀錄（2026-05-12 二）

> 鍾昀臻（Emma）→ 主管 chainwinit
>
> 分支：`feat/uiux-round14-emma`
> 起點：`becd3d3`（5/11 收尾）
> 終點：`93d1186`（今日最新，已 push 到 origin）

---

## 一、今日摘要

- **Round 14 UIUX 進度**：已修正 **109 / 161**（**66.9%** 嚴格 / 73.6% 含部分修正）
  - 比 5/11 收尾時 **53.5%**（84/157）推進 **+13.4 個百分點**
  - 主清單 sheet3：109 已修正 / 11 部分修正 / 43 待處理（4 條增刪）
- **commit 數**：8 個
- **檔案變動**：跨 iFare_Frontend + iFare_Backend + docs + scripts
- **新增 docs**：`iFare_後台優化規劃_2026-05-12.md`（給主管討論方向）

---

## 二、Commit 時間軸

| 時間 | hash | 主題 |
|---|---|---|
| 10:32 | `ab54066` | UIUX 第四批 i-Fare 收尾 6 條（#2/#11/#12/#13/#86/#87）+ Codex 第三輪殘留收尾 |
| 10:41 | `e99e50f` | revert #163 移除動態島手機選單入口（依 Emma 反饋） |
| 10:55 | `5a430fc` | chatbot 退出按鈕 + 福利專欄 chip RWD + 全站 NuxtLoadingIndicator + articles 搜尋中提示 |
| 11:15 | `990f8b9` | revert chatbot 手機版改回浮窗（不再滿版） |
| 11:15 | `0edc1c2` | UIUX 第五批 6 條（#67/#68/#70/#73/#82/#116） |
| 11:16 | `321aa91` | docs：產出後台優化規劃 markdown |
| 11:28 | `1311402` | 補回靈動島 hover 效果 — 套到 i-Fare 系列 4 個卡片 |
| 13:52 | `93d1186` | Round 14 第六輪 — Codex 後端/SEO/Chatbot/後台大批推進 + Emma xlsx 整理 |

---

## 三、今日工作分類

### 1. 前端 UIUX 修正（i-Fare 收尾 + 第五批）

**第四批（#2/#11/#12/#13/#86/#87）**— commit `ab54066`：

- `#2` 受助對象篩選器進場動畫：`isVisibleRecipient` 預設 false，選了情境後才淡入
- `#11` 洽辦單位拆乾淨：拿掉 `<div @click>`、`<label>` → `<span>`、跳頁 button 補 type/aria-label/@click
- `#12` 多通道社群分享：新建 `composables/useShareUrl.ts`，補 FB / Email / 複製連結 3 通道
- `#13` contact 手機改 chip 水平滑動列表（與桌面一致）
- `#86` result 空狀態：圖示 + 引導文案 + 兩顆 CTA
- `#87` FAQ focus-visible：outline + box-shadow，對比 .35 → .7 符合 WCAG

**第五批（#67/#68/#70/#73/#82/#116）**— commit `0edc1c2`：

- `#67` 圓角不一致（部分修正）：新建 `assets/style/_design-tokens.scss` 收 `$radius-xs/sm/md/lg/xl/2xl/pill/circle/bg-*` 9 個語意化變數
- `#68` 陰影深度（部分修正）：同檔加 `$shadow-rest/card/hover/lift/cta/focus` 6 個變數
- `#70` 缺 768px 斷點：補 `$MAX_WIDTH_TABLET: 768px`
- `#73` 高度動畫 layout 重繪（部分修正）：示範 `.item-recipient` 從 height 改 opacity + transform + max-height
- `#82` 404/500 頁面：新建 `error.vue` 含 4 種 statusCode + 3 顆 CTA + RWD
- `#116` CompPage isHide bug：重寫 `recomputeVisibleWindow()`，修上一頁 page 拉不回 bug

### 2. Chatbot 改進

- commit `5a430fc`：補退出聊天按鈕（雙保險：header X 補 aria-label/title + 底部新增明顯文字按鈕）+ 手機 safe-area-inset-top
- commit `990f8b9`：手機版改回浮窗，不再滿版（reverted 自身上一版的滿版設計）
- commit `93d1186`：Codex 第四輪 chatbot 大改造（CompChatbotWelcome +1248 行 / Entry +102 行 / chatbot.post.ts +280 行），對應 sheet3 #139-#142

### 3. SEO / API 統一（commit `93d1186` 內）

- `#146` `nuxt.config.ts` URL 環境變數化：`RESOLVED_SITE_URL` 統一三處 (`runtimeConfig.public.siteUrl` / `site.url` / SSR base)
- `#147` 詳情頁 `index:false` 議題：移除部分 page 的排除規則
- `#145` 新建 `composables/useApiResult.ts`：提供 `getApiResultValue/Array/Item` 三個 helper，各 page 套上避免 `res.result.result` 直取造成白畫面

### 4. 後台管理介面（iFare_Backend）— commit `93d1186` 內

依 sheet2 後台 backlog 第一階段條目：

- `CardSearchParam.vue` +147：對應 sheet2 #20 搜尋清除 / #26 日期快捷
- `CardTable.vue` +100：對應 sheet2 #17 表格列寬 / #42 批次操作
- `HomeView.vue` +365：對應 sheet2 #39-#41 + #53 Dashboard 重構
- `PageManagement` AddEdit / DataList：CMS 強化

### 5. 視覺一致性

- commit `e99e50f`：revert #163 動態島手機選單（Emma 反饋不需要）
- commit `1311402`：補回靈動島 hover 效果——
  - `_mixin.scss` 新增 `$ease-island = cubic-bezier(0.32, 0.72, 0, 1)` + `@mixin island-hover`
  - 套到 i-Fare 系列 4 個卡片：result-item / relation-item / agency-item / faq-item
  - 過去只有 article-item / news-item / about 執行長框有，現在 i-Fare 系列一致化

### 6. 文件產出

- **新檔**：`docs/iFare_後台優化規劃_2026-05-12.md`（commit `321aa91`）
  - 依 sheet2 54 條後台 backlog 分三階段（立即可做 / 中期重構 / 長期架構改造）
  - 含跨議題 5 條 + 跟主管討論點 5 條
  - **這份是給主管討論的主要文件**

---

## 四、xlsx 進度（`docs/iFare_問題追蹤與AI維運規劃.xlsx` 主清單 sheet3）

| 狀態 | 5/11 收尾 | 今日終 | Δ |
|---|---|---|---|
| 已修正 | 84 | 109 | +25 |
| 部分修正 | 8 | 11 | +3 |
| 待處理 | 65 | 43 | -22 |
| **合計** | **157** | **163** | +6（新增條目） |
| **嚴格完成率** | **53.5%** | **66.9%** | **+13.4 pp** |

---

## 五、環境現況

- **branch**：`feat/uiux-round14-emma` HEAD `93d1186` 與 origin 同步、working tree clean
- **前端 Nuxt dev server**：http://localhost:3000（中午之後正常）
- **後台 Vite dev server**：http://localhost:5173/**index.html**（重要：因 `vite.config.ts:11` `base: './'`，根路徑 `/` 會回 404，要明確帶 `/index.html`）

---

## 六、待主管決策 / 後續

1. **`feat/uiux-round14-emma` 是否 merge 進 master？**
   - 109 條已修、66.9% 完成率，commit log 清楚
   - 主要 risk：第六輪 `93d1186`（Codex 大批 + Emma xlsx 整理）尚未逐檔 review，commit msg 標明「未逐檔 verify」
2. **後台優化方向**（看 `docs/iFare_後台優化規劃_2026-05-12.md`）
   - 第一階段 6 條（~2.6 days）是否 Emma 接手開做？
   - 第二階段 #1 CRUD 高層共用模板 + #7 WebAPI 拆分要不要做？
   - 第三階段權限改寫 #6 影響面大，需主管同意
3. **下批 UIUX 排程**（記憶中 5/11 制定的 multi-week plan）
   - 5/18-19 原訂：第五批 福利專欄 + about（#32-35, #40, #42, #25, #39）
   - 第七批 設計系統（#64-68, #76）需先開會
4. **跨領域議題**（建議跟後端 / DevOps 開 30 分鐘討論）
   - 後台 #8 API 位址寫死於 `AjaxRef` → 環境變數化
   - 後台 #14 baseURL 暫時硬寫
   - 後台 #16 Visitor SetVisitorRecord prod 404（前台 console 一直噴錯）
   - 後台 #9 token 生命週期

---

## 七、已知議題

- **`docs/iFare_問題追蹤與AI維運規劃.xlsx` 容易脹大**：xlsx-js-style 不壓縮 `xl/theme/theme1.xml`，每跑一次 update script 會養肥 10MB+。已有 workaround script：`scripts/compact-xlsx-theme.mjs`（每次 update 後手動跑一次壓回）+ `scripts/style-uiux-tracking-status-colors.mjs`（狀態欄上色）。當前 size 178.6 KB，正常。
- **vite.config 後台 dev base 是 `./`**：訪問 `http://localhost:5173/` 會 404，需 `localhost:5173/index.html`。若想長期解決，改 `base: process.env.NODE_ENV == 'production' ? '/ifare_backend/' : '/'`。今日未動。
- **commit `93d1186` 細節未逐檔 verify**：Codex 第六輪改動 27 檔（含後端管理介面 +1000 行），commit message 是依 diff stat 推測 scope。實際對應 xlsx 哪些條目需要主管 / Emma 後續對照。
- **記憶提醒**：「Emma 只 push，主管才 merge」——所有 commit 已 push 但沒開 PR、沒 merge。

---

## 八、附錄：相關文件

- 待修正項目盤點：`docs/iFare_問題追蹤與AI維運規劃.xlsx`（sheet3 = 前台 UIUX / sheet2 = 後台 backlog）
- 後台規劃詳細版：`docs/iFare_後台優化規劃_2026-05-12.md`
- 系統架構：`docs/iFare_前後端架構圖.md` + `diagrams/`
- 每日工作日誌（停在 4/14）：`C:\Users\emma.chung\Desktop\work\iFare_任務清單_鍾昀臻.md`

---

## 九、2026-05-19 補充：UIUX + 後台 + 文件收尾

> 本段為 5/19 工作補充，保留 5/12 原交接內容，不覆蓋歷史紀錄。

### 1. 目前分支與提交狀態

- **branch**：`feat/uiux-round14-emma`
- **目前 HEAD**：`be7370d`（`feat: finalize uiux and page builder fixes`）
- **目前狀態**：程式修正已 push；本輪文件補齊中的 `.md` / `.docx` 仍在 working tree，尚未 commit / push。

### 2. 今日已完成重點

- **PageBuilder 前台顯示鏈路補強**
  - 後台新增 / 編輯頁儲存後可同步到前台 `/<slug>`。
  - 前台使用 Nuxt server route 讀取 `server/data/dynamic-pages.json`。
  - 已補 `docs/iFare_PageBuilder_前端顯示PoC_2026-05-18.md` 到 PoC v3。
- **圖片上傳與前台破圖修正**
  - 後台 PageBuilder 圖文區塊可上傳本機圖片。
  - 前台以 `/api/dynamic-assets/<filename>` 顯示圖片。
  - 阻擋直接貼 `C:\...` 本機路徑造成正式站無法讀取。
- **後台 UIUX 收尾**
  - 搜尋清除、日期快捷、刪除確認、Dashboard RWD、登入 Caps Lock 提示、無權限頁等已完成或補強。
  - PageBuilder 前台預覽已能依桌機 / 平板 / 手機切換版面。
- **文件同步**
  - 已補：PageBuilder PoC、新增頁面優化建議、專案總覽、部署維運、開發上手。
  - 已同步產生 / 更新對應 `.docx`，目前 Word 版與 Markdown 版一致。

### 3. 遠端主機部署前注意

- 遠端主機不建議直接手改檔案；建議本機完成、push 到 GitHub，再由遠端拉版部署。
- PageBuilder 目前 runtime 資料在前台：
  - `server/data/dynamic-pages.json`
  - `server/data/dynamic-assets/`
- 若部署流程會整包覆蓋前台目錄，必須先規劃 `server/data/` 的持久化、備份與還原，否則動態頁與圖片會消失。
- 後台需設定或確認：
  - `VITE_FRONTEND_BASE`
  - `VITE_FRONTEND_SYNC_URL`
  - `VITE_FRONTEND_ASSET_UPLOAD_URL`

### 4. 建議下一步

1. 將本輪文件變更 commit / push。
2. 上遠端主機前先確認 build：
   - 前台：`npm run build`
   - 後台：`npm run type-check`、`npm run build`
3. 遠端主機 smoke test：
   - 後台登入
   - PageManagement 新增頁面
   - 圖片上傳
   - 前台 `/<slug>` 顯示
   - 手機 / 平板 / 桌機 RWD 檢查
4. 暫時不要更新 `API文件` / `資料庫文件`，除非正式新增 .NET API 或 DB schema。

---

_由 Claude Opus 4.7 協助 Emma 整理。如需進一步說明請對照 `git log feat/uiux-round14-emma --since='2026-05-12' --no-merges`。_
